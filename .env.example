# Optional — enables MODE B (live environmental data). Get a free token at
# https://aqicn.org/data-platform/token/ and read lib/liveEnvironment.ts for
# the legal/usage constraints (non-commercial, no caching/archiving of the
# raw feed, attribution required) before using this beyond local
# development or research demonstration.
# WAQI_TOKEN=
